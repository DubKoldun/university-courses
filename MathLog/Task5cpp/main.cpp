#include <iostream>

#include "utils/proof.hpp"
#include "utils/expression-utils.hpp"

int main() {
    initialize();

    // /* parse task */
    // proof pr(/* hypotheses */, /* result */);
    //
    // try {
    //     while (/* read a line */) {
    //         /* parse */
    //         pr.append(/* expr */);
    //     }
    // } catch (const proof_exception& e) {
    //     std::cout << e.what() << std::endl;
    //     return 1;
    // }
    // 
    // std::cout << (pr.correct() ? "Proof is correct" : "Required hasn't been proven") << std::endl;
    return 0;
}
