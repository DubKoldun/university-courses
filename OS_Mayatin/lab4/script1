#!/bin/bash

mkdir ~/test && echo "catalog test was created successfully" >> ~/report.txt && touch ~/test/$(date '+%d.%m.%y_%H:%M:%S')
ping -v -c 1 www.net_nikogo.ru 2> /dev/null || echo "ERROR" >> ~/report.txt